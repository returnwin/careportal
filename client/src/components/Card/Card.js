import React, { PureComponent } from 'react';
// import './Card.css';

class Card extends PureComponent {

  render() {

    return (
      <div>
        Card component
      </div>
    );
  }
}

// Card.propTypes = {
// }

export default Card;